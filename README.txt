Test repo for filename encoding issues

This repo exists to test various filename encoding scenarios in Git
repos. For example, what happens if a UTF-8 filename is created in
Linux, then checked out on a Windows filesystem?
